language: python
python:
- '3.6'
services:
  - postgresql
before_script:
  - psql -c "create database test_db;" -U postgres
  - export APP_SETTINGS=testing  

install:
- pip install -r requirements.txt

script:
- pytest --cov
- coverage
- coveralls
- coverage report
after_success:
- coveralls